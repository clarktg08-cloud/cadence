{
  "name": "cadence",
  "version": "0.1.0",
  "private": true,
  "description": "Cadence — a lightweight job application tracker (vanilla HTML/CSS/JS, no build step).",
  "scripts": {
    "start": "serve .",
    "dev": "serve ."
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
